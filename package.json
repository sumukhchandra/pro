{
  "name": "freemium-content-platform",
  "version": "1.0.0",
  "description": "A freemium content platform with reader and creator hubs",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run web\"",
    "server": "cd apps/server && npm run dev",
    "web": "cd apps/web-next && npm run dev",
    "mobile": "cd apps/mobile && npm start",
    "install-all": "npm install && cd apps/server && npm install && cd ../web-next && npm install && cd ../mobile && npm install"
  },
  "keywords": ["content", "platform", "freemium", "reader", "creator"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}